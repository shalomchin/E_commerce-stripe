  <p id="notice"><%= notice %></p>
  <div class = "container-fluid row">
    <p>
      <span id="chicks"></span>
    </p>
    <p>
      <strong>Name:</strong>
      <%= @product.name %>
    </p>

    <p>
      <strong>Price:</strong>
      <%= number_to_currency @product.price %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= @product.description %>
    </p>

    <p>
      <strong>Stock:</strong>
      <%= @product.stock %>

      <br>
  <!-- form_for path  
  <form action="/submmiting_path"></form>
-->
<!-- form_for Order.new, url: "/products/#{@product.id}/orders" do -->
<!-- form_for Order.new, url: product_orders_path do -->
<!-- form_for [@product, Order.new] do -->
<!-- form_for product_orders_path(@product) do -->
<!-- product needs @ --> 


<!--   <%= render 'orders/paymentbutton' %> -->
  
    <%= render 'line_items/form' %>
  
    
 <br>

  <!-- <%= link_to 'Edit', edit_product_path(@product), class: "btn btn-primary" %> | -->
  <%= link_to 'Delete', product_path, method: :delete, data: { confirm: 'Are you sure?'}, class: "btn btn-danger" %> | 
  <%= link_to 'Back', products_path, class: "btn btn-default" %>  
</div>
